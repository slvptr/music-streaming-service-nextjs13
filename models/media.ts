export type Playlist = {
  title: string;
  description: string;
  coverUrl: string;
  playlistId: string;
};

export type Track = {
  title: string;
  artist: string;
  coverUrl: string;
  trackUrl: string;
};
